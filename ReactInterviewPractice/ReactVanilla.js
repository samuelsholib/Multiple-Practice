const namesArray = [  "Yacob", "Samuel", "Jeremiah", "Emmanuel", "Dinku" ]

function consoleNames() {
    console.log(namesArray);
}

consoleNames();

const listOfNames = () => {
    const listItems = namesArray.map((name) =>
    // <li key={name}>
    //     {name}
    // </li> 
            {listItems}
    );
}
